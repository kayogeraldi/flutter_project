import 'package:flutter/material.dart';

class CatalizadorScreen extends StatelessWidget {
  const CatalizadorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
