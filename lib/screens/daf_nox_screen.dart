import 'package:flutter/material.dart';

class DafNoxScreen extends StatelessWidget {
  const DafNoxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
